# Wikipedia-Recovery-System
A lightweight information retrieval system that indexes and searches Wikipedia articles stored in JSON format. The project implements core IR components—tokenization, inverted indexing, boolean search, and optional positional and semantic search—by completing the SAR_lib.py library. Designed as part of the SAR–ALT coordinated coursework.
